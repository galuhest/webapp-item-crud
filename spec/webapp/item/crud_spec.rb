RSpec.describe Webapp::Item::Crud do
  it "has a version number" do
    expect(Webapp::Item::Crud::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
